/**
 * Main Game Entry Point
 * 
 * Initializes the Phaser game with the configuration
 */

// Create the Phaser game instance
const game = new Phaser.Game(gameConfig);

// Log initialization
console.log('Northern Journey - Game initialized');
